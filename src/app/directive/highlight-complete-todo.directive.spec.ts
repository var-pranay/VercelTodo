import { HighlightCompleteTodoDirective } from './highlight-complete-todo.directive';

describe('HighlightCompleteTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompleteTodoDirective();
    expect(directive).toBeTruthy();
  });
});
